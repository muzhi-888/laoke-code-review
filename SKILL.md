---
name: laoke-code-review
slug: laoke-code-review
displayName: 代码审查助手
version: 1.0.0
author: 局内人·老K
description: 通用代码审查助手，覆盖安全注入密钥硬编码越权性能可维护性六维，内置静态红线扫描器，输出分级审查报告
compatibility: 通用（支持 Skill 的 Agent：Claude Code / CodeBuddy / DeepSeek / WorkBuddy 等）
permissions: 读取源码目录/文件、运行内置 Python 静态扫描脚本、输出 Markdown/JSON 审查报告
safety: 仅用于合法合规的代码质量审查；内置护栏硬拒恶意代码绕过安全检测未授权入侵等违规请求
---

# 代码审查助手

> 这个 Skill 是给写代码的人配的"第二个 reviewer"。它不替你写代码，而是把你刚写完、准备提交、或准备合并的改动，按工程审查的标准过一遍——先机械地捞出最致命的红线（密钥硬编码、SQL 拼接、裸 except、eval 外部输入、弱哈希、调试残留），再按"安全 / 正确 / 可维护 / 可读 / 性能 / 可测试"六维给你一份能直接拿去改的问题清单和报告模板。
> 适用对象：个人开发者、技术负责人、需要频繁做 Code Review 的团队、接手遗留代码想先体检的人、带新人做代码规范的建设者。

---

## 一、这个 Skill 能干什么（能力边界）

**能做：**
1. 静态红线扫描：对一个目录或单文件跑 `scripts/review_scan.py`，自动识别硬编码密钥、SQL 拼接注入、裸 `except`、弱哈希、eval/exec、调试残留、过长函数等，输出分级报告（Markdown / JSON）。
2. 六维人工审查引导：按"安全 / 正确 / 可维护 / 可读 / 性能 / 可测试"六维，给你逐项审查清单（见 references/01）。
3. 安全快查：把 OWASP 高频漏洞对应到"这段代码里长什么样、该怎么改"（见 references/02）。
4. 性能反模式识别：N+1、循环内网络请求、全表加载再过滤、事件循环阻塞等（见 references/03）。
5. 可维护性规则落地：命名、函数粒度、魔法数字、重复代码、注释（见 references/04）。
6. 生成分级审查报告：按 CRITICAL/HIGH/MEDIUM/LOW 给每条问题标严重度、写清"为什么"和"怎么改"（见 references/05）。

**不能做（护栏）：**
- 不替你做最终合并决策——改不改、怎么改是人和团队的权责。
- 不提供绕过安全检测、隐藏后门、规避扫描器的任何方法（这类请求被硬拒，见 hooks/guardrail.md）。
- 不写恶意代码、攻击工具、未授权入侵代码。
- 静态扫描覆盖不了"逻辑正确性 / 业务匹配 / 隐性性能瓶颈"，这些必须人工 review——本 Skill 会明确告诉你边界在哪里，不夸大自己。

> 经验之谈：好的 Code Review 不是"找 bug"，是"降低未来改这段代码的人踩坑的概率"。所以本 Skill 的重点永远是"可执行的修改建议 + 风险说明"，而不是"这里不对"四个字。

---

## 二、推荐的审查工作流

不管是审自己的代码还是别人的 PR，都按这个顺序走：

### 第 1 步：跑静态红线扫描（机器先过）
```bash
# 扫描整个目录（递归）
python scripts/review_scan.py ./src --format md

# 只看最严重的 N 条
python scripts/review_scan.py ./src --top 20

# 接 CI 流水线用 JSON
python scripts/review_scan.py ./src --format json > review.json
```
扫出来的 CRITICAL / HIGH 先处理——这些是"提交前就该拦"的硬伤，不依赖人的判断。

### 第 2 步：按六维做人工审查
拿着扫描结果，再对照 references/01 的清单，逐维问自己：
- 安全：外部输入都校验/转义了吗？权限每个入口都查了吗？
- 正确：边界、空值、异常路径处理了吗？
- 可维护 / 可读：命名、函数粒度、注释到位吗？
- 性能：有 N+1、循环内 IO 吗？
- 可测试：核心逻辑解耦了吗？补测试了吗？

### 第 3 步：写分级报告
按 references/05 的模板，把问题分成"必须改 / 建议改 / 讨论"，每条给位置、问题、建议、理由。报告里保留一条声明："静态扫描已通过 ≠ 无问题，逻辑与设计仍需人工 review。"

### 第 4 步：跟进与闭环
- 把报告贴回 PR / 发给作者。
- 关键问题改完，再跑一次扫描确认红线清零。
- 争议点（主观偏好类）留给作者权衡，不强行卡。

---

## 三、静态扫描器使用详解

扫描器是纯标准库、零依赖的 Python 脚本，支持：Python（AST 深度分析）、以及 JS/TS/Java/Go/Ruby/PHP/C/C++/Rust/Swift/SQL/HTML 等源码的通用正则扫描。

### 参数
```
python review_scan.py <路径> [--format md|json] [--top N] [--max-fn 60] [--max-file 600]
```
- `<路径>`：单文件或目录（目录递归，自动跳过 node_modules / .git / __pycache__ / 二进制）。
- `--format`：md（人读）或 json（流水线）。默认 md。
- `--top N`：只显示严重度最高的前 N 条。
- `--max-fn`：函数超过多少行算"过长"，默认 60。
- `--max-file`：文件超过多少行算"过大"，默认 600。
- 退出码恒定 0（它是报告器，不是测试门禁；若当 CI 门禁用，按 JSON 的严重度字段自行决定是否 fail）。

### 它能识别的规则（命中即报）
| 规则 | 严重度 | 说明 |
|------|--------|------|
| hardcoded-secret | CRITICAL | `password/api_key/token/secret = "..."` 字面量 |
| sql-injection | CRITICAL | SQL 语句疑似字符串拼接用户输入 |
| eval-exec | HIGH | 执行外部构造的字符串 |
| weak-hash | HIGH | MD5 / SHA1 弱哈希 |
| bare-except | HIGH | 裸 `except:` 吞掉所有异常 |
| debug-residue | MEDIUM | pdb / ipdb / breakpoint 残留 |
| broad-except | MEDIUM | 捕获过宽的 Exception |
| mutable-default | MEDIUM | 函数用 `[]` / `{}` 默认参数 |
| long-function | MEDIUM | 函数体超过阈值 |
| large-file | LOW | 文件过大 |
| debug-print | LOW | print / console.log 调试残留 |
| todo-marker | LOW | TODO / FIXME / HACK 遗留 |
| assert-in-prod | LOW | 生产依赖 assert 校验 |
| syntax-error | MEDIUM | 文件语法错误，无法解析 |

### 输出示例（Markdown）
```
# 代码审查静态扫描报告

- 扫描文件数：12
- 代码总行数：1843
- 发现问题：7 条

严重度分布：CRITICAL=2  HIGH=2  MEDIUM=2  LOW=1

## [CRITICAL] hardcoded-secret — src/auth.py:18
- 问题：疑似硬编码密钥/密码/令牌
- 建议：改从环境变量或密钥管理服务读取，切勿入库

## [HIGH] bare-except — src/handler.py:42
- 问题：裸 except: 吞掉所有异常
- 建议：捕获具体异常类型，至少 except Exception 并做处理
...
```

### 已知局限（务必告诉用户）
- 扫描基于"行 + 轻量 AST"，SQL 注入只在拼接与 `execute` 同行的情形最稳；跨变量拼接需人工确认。
- 它认不出"逻辑是否正确""业务是否匹配"，只认"静态特征"。所以**报告末尾永远附一句：静态扫描通过不等于安全，设计层需人工 review**。

---

## 四、六维审查速记（完整清单见 references/01）

- **正确性**：需求分支都覆盖？空值/超长/异常路径？循环终止？浮点别用 `==`？
- **安全性**：外部输入校验转义？SQL 参数化？密钥没硬编码？反序列化可信源？权限每入口都查？没用 eval 外部输入？
- **可维护性**：函数单一职责？命名表意？魔法数字提常量？重复超 3 处抽函数？无循环依赖？
- **可读性**：复杂表达式拆变量？注释讲"为什么"？无大段死代码？布尔别嵌套（用提前返回）？
- **性能**：无 N+1？无循环内网络请求？不过载全表？async 里不阻塞？
- **可测试性**：核心逻辑与 IO 解耦？补了测试？测试不依赖全局状态/真实外部服务？

记忆口诀：**安→正→维→读→性→测**，前两个一票否决。

---

## 五、安全红线速记（完整见 references/02）

> 拼字符串进 SQL / 命令 = 死
> 密钥写源码里 = 死
> eval 外部输入 = 死
> 裸 except 吞异常 = 死
> 权限只查登录不查归属 = 死

五条命中任意一条，安全维度不过关，必须退回修改。

---

## 六、典型场景与处置

### 场景 A：提交前自查
用户："帮我把 ./src 过一遍，准备提 PR。"
→ 跑 `review_scan.py ./src --format md --top 30` → 先处理 CRITICAL/HIGH → 对扫描器没覆盖的维度，按 references/01 给 3-5 条最关键的人工审查建议 → 输出报告。

### 场景 B：审别人的 PR
用户贴来一段 diff 或一个文件。
→ 若给的是本地路径，直接跑扫描器；若贴的是代码片段，人工按六维逐条看，重点查安全与正确（参考 references/02/03）→ 按 references/05 模板写意见：每条给位置(行号)、问题、建议、严重度。

### 场景 C：接 CI 门禁
用户："我想在流水线里卡安全红线。"
→ 用 `--format json`，按 JSON 里 `counts.CRITICAL` / `counts.HIGH` 是否 > 0 决定是否 fail 构建。注意：扫描器只卡静态红线，逻辑/业务问题 CI 拦不了，需在 PR 模板里要求人工 review。

### 场景 D：带新人做规范
用户："我们团队代码规范太松，帮我出一份审查要点。"
→ 把 references/01~05 浓缩成一份团队审查清单（按六维 + 严重度），强调"对事不对人、给方案不给命令"的审查礼仪（references/05 第四节）。

### 场景 E：接手遗留代码体检
用户："这堆老代码我不敢动，先看看有多烂。"
→ 跑扫描器拿红线数量 → 按严重度排序，先列"最该立刻修的 5 条" → 说明哪些是可维护性问题（渐进改）、哪些是安全隐患（必须立刻改）→ 给分阶段改造建议，别一上来让作者重写。

---

## 七、输出格式规范

给用户的最终审查交付，建议包含：

```markdown
# 代码审查报告：<主题>

## 总评
<可合并 / 需修改后合并 / 需讨论>

## 统计
CRITICAL: n  HIGH: n  MEDIUM: n  LOW: n

## 必须修改（CRITICAL/HIGH）
1. [文件:行] 问题 + 建议 + 理由

## 建议修改（MEDIUM）
- ...

## 讨论 / 疑问（INFO）
- ...

## 亮点
- <做得好的地方>

> 声明：本报告由静态扫描 + 六维人工审查生成，覆盖安全/正确/可维护/可读/性能/可测试。
> 静态扫描通过 ≠ 无问题，逻辑正确性与业务匹配需人工 review 确认。
```

---

## 八、异常处理与边界

- **代码扫描不了（编码/权限）**：扫描器对读不了的文件记 `read-error`（LOW），不中断整体；告知用户哪些文件被跳过。
- **语法错误**：Python 文件语法错误时，AST 层跳过，记 `syntax-error`（MEDIUM），并退化为正则扫描，避免漏掉密钥类红线。
- **超大仓库**：目录扫描会递归所有文本源码，二进制自动跳过；若太慢，让用户指定子目录或用 `--top` 只看最严重。
- **非源码文件**：图片/压缩包/锁文件不在扫描范围，直接忽略。
- **敏感代码不外传**：提醒用户扫描在本地文本上进行，不外发任何数据；涉及机密代码不要粘贴到外部聊天框。
- **扫描器误报**：密钥规则对"测试用字符串字面量"也可能报警——这是故意保守，让用户自己确认是否为真密钥；可在报告里标注"疑似"，由人判断。

---

## 九、质量自检（本 Skill 自己的红线）

- 每条 CRITICAL / HIGH 意见必须带"为什么危险 + 具体改法"，不能只说"不安全"。
- 绝不因为"用户说没事"就放行安全红线（注入 / 密钥硬编码 / 越权 / eval 外部输入）。
- 明确告知能力边界，不把"静态扫描通过"包装成"代码安全"。
- 审查意见对事不对人，给方案不给命令。

---

## 十、使用示例

**示例 1（命令行自查）**
```bash
python scripts/review_scan.py ./my_project --format md --top 20
```
输出一份按严重度排序的红线清单，先改 CRITICAL/HIGH 再提 PR。

**示例 2（人工审查一条 PR 意见）**
```
[HIGH] payment.py:77
问题：支付回调只校验了"是否登录"，没校验"当前用户是否拥有这笔订单"，存在越权（IDOR）。
建议：在查订单后加一道归属校验 `if order.user_id != current_user.id: abort(403)`。
理由：前端隐藏按钮不等于后端授权，任何人改 URL 里的 order_id 就能看别人的订单。
```

**示例 3（CI 门禁片段）**
```bash
python scripts/review_scan.py ./src --format json > review.json
# 读取 review.json 的 counts.CRITICAL / counts.HIGH，>0 则 fail 构建
```

**示例 4（遗留代码体检结论）**
```
体检结论：12 个文件，7 条红线（2 CRITICAL / 2 HIGH / 2 MEDIUM / 1 LOW）。
最该立刻修的：auth.py:18 硬编码密钥、handler.py:42 裸 except。
可维护性债（渐进改）：3 个函数超 60 行、1 处复制粘贴超 3 次。
建议：先清 2 条安全红线，再排期重构，不必一次性重写。
```

---

## 十一、与其他 Skill 的配合

- 写完代码用本 Skill 审查 → 发现问题改完 → 用测试/构建类 Skill 跑门禁 → 再合并。
- 本 Skill 偏"质量门禁"，不写代码、不生成测试；需要生成单测可配合对应测试生成能力。
- 性能问题若扫描器标了 MEDIUM 但你想确认是否真瓶颈，需用性能剖析工具（profiler）实测，本 Skill 不替代压测。

---

## 十二、隐私与数据说明

- 审查完全在本地文本上进行，本 Skill 不发起任何网络请求、不上传任何代码。
- `review_scan.py` 只读你指定的文件，输出报告到终端或你指定的文件，不外传。
- 若你通过支持联网的 Agent 使用本 Skill，包含密钥、商业逻辑的敏感代码建议在本地终端直接跑脚本，不要粘贴到任何外部界面。
- 扫描器的密钥规则偏保守（测试字符串也会报警），由你最终判断是否为真密钥，避免误提交也避免漏报。

---

## 十三、分语言审查要点

不同语言的"高发雷区"不同，下面是各语言最该盯的几类。

### Python
- 异步函数里用同步阻塞调用（`time.sleep`、`requests.get`）会卡事件循环 → 改 `await asyncio.sleep` / `aiohttp`。
- 可变默认参数 `def f(x=[])` → 改成 `x=None` 内部初始化。
- `except:` 裸捕获 → 至少 `except Exception`。
- `assert` 做参数校验 → 生产 `-O` 下失效，改显式异常。
- 用 `pickle.loads` 反序列化不可信数据 → RCE 风险，换安全格式（json / marshmallow）。
- 路径拼接用字符串 `+` → 改 `os.path.join` / `pathlib`，避免跨平台与穿越。

### JavaScript / TypeScript
- `eval()` / `new Function()` 执行外部字符串 → 注入，禁用。
- `==` 弱相等（`'0' == 0` 为 true）→ 一律 `===`。
- `innerHTML` 直接拼用户输入 → XSS，改 `textContent` 或转义库（DOMPurify）。
- `JSON.parse` 不包 try → 异常抛到上层，需 try/catch。
- 回调地狱 / 未处理的 Promise rejection（`await` 没 try）→ 显式处理错误。
- TS 里用 `any` 绕过类型 → 至少 `unknown` + 收窄。
- `console.log` 调试残留 → 改日志框架或删除。

### Go
- 忽略 error（`_ = doSomething()` / `doSomething()` 不接 err）→ 至少记日志或返回。
- 整型溢出（无符号减法下溢、int 与 int32 混用）→ 显式类型与边界检查。
- `defer` 在循环里累积资源 → 可能 OOM，移出循环或显式关闭。
- 竞态：共享 map 无锁并发读写 → 用 `sync.Map` 或 `mutex`。
- `panic` 当控制流 → 仅用于不可恢复错误，业务错误用返回 error。

### Java
- `null` 不判直接解引用 → NPE；用 `Optional` 或显式判空。
- 资源不关（`Connection` / `InputStream` 没 `close`）→ 用 try-with-resources。
- `==` 比字符串 → 用 `.equals()`。
- 反序列化不可信对象（`ObjectInputStream`）→ 反序列化注入，限制白名单类。
- SQL 用字符串拼接 `Statement` → 改 `PreparedStatement` 参数化。

### SQL（含于应用代码）
- 字符串拼接拼条件 → 参数化 / 预编译。
- `SELECT *` 无必要 → 只取所需列，减少 IO 与耦合。
- 无索引的大表 `WHERE` / `JOIN` → 审查执行计划，确认命中索引。
- 事务过大（长事务持锁）→ 缩小事务边界。

---

## 十四、Reviewer 侧的反模式

审查者自己也会犯错，下面这些让 review 变无效甚至有害：

1. **只看风格不看过不过**：括号换行挑半天，注入漏洞没看见。顺序永远是"安全/正确 > 可维护/可读 > 风格"。
2. **全是"建议"没底线**：该卡的 CRITICAL 也写成"可改可不改"，等同放行。分级必须硬。
3. **替作者把每一行都写了**：review 给方向，不抢实现权。除非明显错误，让作者自己改。
4. **一次性甩 30 条没有优先级**：作者无从下手。先给"必须改"的 3-5 条，其余排期。
5. **对事不对人做反了**：说"你又没判空"而非"这段代码空输入会崩"。措辞影响执行意愿。
6. **LGTM 式敷衍通过**：没真看就批准。CI 过了不代表逻辑对，人工 review 的价值就在 CI 之外。
7. **只挑刺不点赞**：做得好的地方也写出来，正向反馈让作者更愿意听意见。
8. **在注释里吵实现哲学**：纯主观偏好标「建议」，别升级成"必须改"的对立。

---

## 十五、PR 提交前自检清单（作者侧）

作者在开 PR 前，自己先过一遍，能砍掉 80% 的来回：

- [ ] 我有没有把密钥 / 密码写死在代码或配置里？（查 `review_scan.py` 的 hardcoded-secret）
- [ ] 所有外部输入（用户参数、文件、回调）都校验/转义了吗？
- [ ] 数据库查询有没有在循环里（N+1）？有没有拼 SQL？
- [ ] 异常处理有没有"裸 except 吞一切"？出错时信息是否还看得见？
- [ ] 函数是不是又写到了 100 行以上、做了好几件事？
- [ ] 有没有 `print` / `console.log` / `pdb` 调试残留？
- [ ] 有没有 `TODO` / `FIXME` 该处理却忘了？
- [ ] 我改的这部分，补测试了吗？至少主路径 + 边界。
- [ ] 命名能不能让不熟这块的人一眼看懂？
- [ ] 我这个改动，有没有改到别处的调用方却忘了同步？

跑一遍 `python scripts/review_scan.py ./改动目录 --format md`，把 CRITICAL/HIGH 清零再提，是最低成本的体面。

---

## 十六、为什么 Code Review 值得做（数据视角）

不堆数字吓人，只说工程上确定的几件事：
- 缺陷在"写完当天"修成本最低，拖到上线后修成本指数级上升（业界常引用的经验区间：修复成本随发现阶段推后放大约 10~100 倍）。
- 大部分严重事故（数据泄露、线上故障）回溯都有"当时有人看到但没卡"的环节——审查是最后一道人肉闸门。
- 团队规范靠 review 沉淀：新人看 review 意见学的，比看文档快。本 Skill 的价值一半在"查问题"，一半在"把规范变成可复用清单"。

所以本 Skill 的定位不是"找茬工具"，是"把团队质量门槛标准化、可复制"的载体。

---

## 十七、常见误报与如何判断

扫描器偏保守，下面这些情况会报警但可能没问题，教用户怎么判：

- **hardcoded-secret 命中测试字符串**：如 `api_key = "test_key_123"`。判断：看它是不是真能用的密钥。测试/示例值可在报告里标"疑似"，由人确认；真密钥无论如何必须移出源码。
- **sql-injection 误报**：扫描器认"拼接 + execute 同行"，但有些 ORM 的 `.raw()` 用了参数占位也会被正则松匹配到。判断：看是否真用了参数化 / 占位符，用了就忽略。
- **long-function 误报**：某些初始化函数（建大量配置）天然长，但逻辑简单。判断：看是否"做了多件事"，只是"列了很多同类赋值"可忽略。
- **debug-print 误报**：日志框架里 `logger.info(...)` 含单词 print？不会（规则只匹配 `print(` 等调用）。放心。
- **todo-marker**：不一定是问题，是"待办提醒"。判断：确认这条 TODO 是否还需处理，是则建 issue，否则删标记。

原则：**保守报警 > 漏报**。宁可让用户多花 10 秒确认，也别漏掉一条真密钥。报告里每条都写"建议"而非"定罪"，把最终判断权留给人和团队。

---

## 附：references 索引

- `references/01_代码审查六维框架.md`：六维完整审查清单。
- `references/02_安全漏洞红线速查.md`：OWASP 高频漏洞 ↔ 代码形态的对应与改法。
- `references/03_性能与并发反模式.md`：N+1、循环内 IO、事件循环阻塞等。
- `references/04_可维护性与可读性规则.md`：命名、函数粒度、魔法数字、注释。
- `references/05_审查报告模板与分级.md`：严重度分级 + 报告模板 + 审查礼仪。
- `scripts/review_scan.py`：纯标准库静态红线扫描器（Markdown/JSON 输出）。
- `hooks/guardrail.md`：合规护栏，硬拒恶意/规避类请求。
